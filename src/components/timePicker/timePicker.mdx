import { Title, Description, Canvas, Meta } from "@storybook/blocks"
import * as TimePickerStories from "./timePicker.stories"

<Meta of={TimePickerStories} />

<Title of={TimePickerStories} />

<Description of={TimePickerStories} />

<Canvas of={TimePickerStories.Default} />

## Usage

```jsx
const handleOnChange = (value: string) => {
  console.log(string)
}

<TimePicker
  value={{ hour: "12", minute: "00" }}
  minuteOptions={["00"]}
  onChange={handleOnChange}
/>
```

## Documentation

- [React Day Picker](https://react-day-picker.js.org)
