import { FormInstance } from "antd"

import { default as Form, FormProps } from "./form.tsx"
import {
  default as FormItem,
  FormItemContent,
  FormItemProps,
} from "./formItem.tsx"
import { default as FormItemWrapper } from "./formItemWrapper.tsx"

export type { FormInstance, FormItemContent, FormItemProps, FormProps }
export { Form, FormItem, FormItemWrapper }
